[project]
name = "upa-dev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=2.24.0",
    "python-dotenv>=1.2.1",
    "pydantic>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
